import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nexus.wmsadmin',
  appName: 'WMS Admin',
  webDir: 'dist/WMS-admin/browser'
};

export default config;
